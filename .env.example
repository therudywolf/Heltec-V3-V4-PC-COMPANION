# Heltec PC Monitor — пример конфига. Скопируй в .env и заполни.
# .env не коммитить в Git!

LHM_URL=http://localhost:8085/data.json

# TCP-сервер (для WiFi на плате)
TCP_HOST=0.0.0.0
TCP_PORT=8888
PC_IP=192.168.1.2
