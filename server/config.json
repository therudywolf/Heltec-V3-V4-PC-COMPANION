{
  "host": "0.0.0.0",
  "port": 8888,
  "lhm_url": "http://localhost:8085/data.json",
  "source": "lhm",
  "prometheus_url": "http://localhost:9182/metrics",
  "gpu_via_nvidia_smi": false,
  "limits": {
    "gpu": 72,
    "cpu": 85
  },
  "weather_city": "London",

  "_comment_claude": "SELF-SET token budgets for the Claude gauge + 80% reminder (Anthropic's real quota isn't on disk). Usage is summed FRESH from ~/.claude session transcripts (input+output, cache excluded) so it's current, not from the stale stats-cache. Sized for that metric (~15-30M heavy day, ~70M/week). 0 disables a gauge.",
  "claude_daily_budget": 30000000,
  "claude_weekly_budget": 100000000,
  "claude_alert_pct": 80,

  "_comment_alerts": "Show the SAME alerts as your Telegram. Either POLL Alertmanager (alertmanager_url -> /api/v2/alerts, no inbound port) OR receive webhooks (alert_webhook_port, POST /alert). Leave alertmanager_url empty to disable polling.",
  "alert_webhook_port": 0,
  "alertmanager_url": "",

  "_comment_forest": "Forest panel node-status scene. Set forest_query_url to any Prometheus /api/v1/query endpoint (e.g. a Grafana datasource proxy) to populate it; empty = scene shows NO NODES. Nodes + PromQL default to forest_panel.DEFAULT_NODES; override with 'forest_nodes'. See SETUP.md.",
  "forest_query_url": ""
}
