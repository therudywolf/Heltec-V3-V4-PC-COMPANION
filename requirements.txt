# Запуск: pip install -r requirements.txt
# NOCTURNE_OS: aiohttp for non-blocking HTTP; heartbeat + hysteresis send
aiohttp>=3.9.0
python-dotenv>=1.0.0
winsdk>=1.0.0b10
Pillow>=10.0.0
psutil>=5.9.0
pystray>=0.19.0
pyinstaller>=6.0.0
